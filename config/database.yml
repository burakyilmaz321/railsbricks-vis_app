# PostgreSQL
#   Ensure the PostgreSQL gem is defined in your Gemfile
#   gem 'pg'
development:
  adapter: postgresql
  encoding: utf8
  database: vis_app_development
  username: ubuntu
  host: localhost
  pool: 5
  port: 5432
  
